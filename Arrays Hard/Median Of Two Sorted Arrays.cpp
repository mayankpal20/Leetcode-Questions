/*vector<int> newvec ;
        
        for(int i = 0 ; i < nums1.size() ; i++){
            newvec.push_back(nums1[i]);
        }
        for(int j = 0 ; j < nums2.size() ; j++){
            newvec.push_back(nums2[j]);
        }

        sort(newvec.begin(),newvec.end());
        double res;
        int sze = newvec.size();
        if( sze % 2 !=  0 ){
            int mid = (sze)/2;
            res = newvec[mid];
        } 
        else{
            res = (double(newvec[(sze/2)-1])+double(newvec[(sze/2)]))/2;
        }
        return res;
        */