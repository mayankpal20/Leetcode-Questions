/*int count = 0 ;
    sort(costs.begin(),costs.end());
    for(int i = 0 ; i < costs.size() ; i++){
        if(costs[i]<=coins && coins>=0){
            coins = coins - costs[i];
            count = count + 1 ;
        }
    }
  if(count!=0){
    return count;
  }
  else{
    return 0;
  }
  */