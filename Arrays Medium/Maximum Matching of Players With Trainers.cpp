/*int count = 0 ;
        sort(players.begin(),players.end());
        sort(trainers.begin(),trainers.end());
        int j = 0 ;
        if(players.size()==1 && trainers.size()==1){
            if(players[j]==trainers[j]){
                return 1;
            }
            else{
                return 0;
            }
        }
        for(int i = 0 ; i < players.size() ; i++){
            while(j<trainers.size()){
                if(players[i]<=trainers[j]){
                    count = count + 1 ;
                    
                    break;
                }
                j++;
            }
            j++;
        }
        return count;

*/