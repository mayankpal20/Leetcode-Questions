//Code which need to be write in a calling method 
/*
sort(nums.begin(),nums.end());
        vector<int> newvector;
        int ans ;
        for(int i = 0 ; i <= (nums.size()-1) ; i++){
            if(nums[i]==target){
                ans = i ;
                newvector.push_back(ans);
            }
        }
        return newvector;
*/