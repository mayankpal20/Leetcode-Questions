/*
vector<int> newvec ;
        int temp = -1 ;
        for(int i = 0 ; i < arr.size() ; i++){
            for(int j = i+1 ; j < arr.size() ; j++){
                if(arr[j]>temp){
                    temp=arr[j];
                }
            }
            newvec.push_back(temp);
            temp=-1;
        }
        return newvec;
        */