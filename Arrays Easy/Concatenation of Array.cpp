/* vector<int> newvec;

        for(int i = 0 ; i < nums.size() ; i++){
            newvec.push_back(nums[i]);
        }


        for(int j = 0 ; j < nums.size() ; j++){
            newvec.push_back(nums[j]);
        }

        return newvec;
        */