/*
vector<int> newvec;
         int i = 0 ;
         if(nums.size()==1){
             newvec.push_back(nums[i]);
         }
         if(nums.size()==2){
             if(nums[i]!=nums[i+1]){
                 newvec.push_back(nums[i]);
                 newvec.push_back(nums[i+1]);
             }
             else{
                 newvec.push_back(nums[i]);
             }
         }
        while(i<nums.size() && nums.size()!=1 && nums.size()!=2){
            if(nums[i]==nums[i+1]){

            }
            else if(nums[i]!=nums[i+1]){
                newvec.push_back(nums[i]);
            }
            
            i++;
        }
        
        nums = newvec;
        return nums.size();
        */