/* vector<int> newvec ;
        int sum = 0 ;
        for(int i = 0 ; i < nums.size() ; i++){
            sum = sum + nums[i];
            newvec.push_back(sum);
        }

        return newvec;
        */