/*
vector<int> newvec ;
        for(int i = 0 ; i < words.size() ; i++){
            string s = words[i];
            for(int j = 0 ; j < s.length() ; j++){
                if(x==s[j]){
                    newvec.push_back(i);
                    break;
                }
            }
        }
        return newvec;
        */