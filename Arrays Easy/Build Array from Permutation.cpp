/* vector<int> newvec ;
        int value = 0 ;
        for(int i = 0 ; i < nums.size() ; i++){
            value =nums[nums[i]];
            newvec.push_back(value);
        }

        return newvec;
        */