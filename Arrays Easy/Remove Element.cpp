/* vector<int> newvec ;
        for(int i = 0 ;i <nums.size() ; i++){
            if(nums[i]!=val){
                newvec.push_back(nums[i]);
            }
        }
        nums = newvec;
        return nums.size();
        */