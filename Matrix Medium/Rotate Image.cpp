/*vector<vector<int>> tempvec(matrix);
        int rowsize = matrix.size()-1;
        int colsize = matrix[0].size()-1;

        for(int col=0 ; col <=colsize ; col++){
            int newrow=col;
            int newcol = 0 ;
            for(int row = rowsize ; row>=0 ; row--){
                matrix[newrow][newcol]=tempvec[row][col];
                newcol++;
            }
        }*/